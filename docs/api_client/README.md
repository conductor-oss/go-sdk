# Moved

This guide now lives at [docs/connection-authentication.md](../connection-authentication.md).
The proxy guide that used to sit beside it was merged into the same page — see
[Proxy Configuration](../connection-authentication.md#proxy-configuration).

This file is a redirect stub, not a guide. It exists solely to keep an external
reference alive: `conductor-oss/conductor` links here from
`docs/documentation/clientsdks/go-sdk.md`. Remove this stub once that reference
points at the new path.

Do not add content here.
